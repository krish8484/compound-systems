name: cs244b CI

on: [push]

jobs:
  cs244b-CI:
    runs-on: ubuntu-latest
    strategy:
      max-parallel: 5

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python 3.9
      uses: actions/setup-python@v3
      with:
        python-version: '3.9'

    - name: Install Miniconda
      uses: conda-incubator/setup-miniconda@v2
      with:
        auto-update-conda: true
        python-version: 3.9

    - name: Create and activate environment
      run: |
        conda create --name cs244b-env python=3.9 -y
        echo "CONDA_ENV_NAME=cs244b-env" >> $GITHUB_ENV
      shell: bash

    - name: Install dependencies
      run: |
        $CONDA/bin/activate $CONDA_ENV_NAME
        pip install -r requirements.txt
      shell: bash

    - name: Lint with flake8
      run: |
        $CONDA/bin/activate $CONDA_ENV_NAME
        flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
        flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
      shell: bash

    - name: Start worker server
      run: python3 worker_server.py 50054
      background: true

    - name: Start scheduler server
      run: python3 scheduler_server.py
      background: true

    - name: Wait for servers to start
      run: sleep 10

    - name: Test with pytest
      run: |
        $CONDA/bin/activate $CONDA_ENV_NAME
        cd tests
        pytest
      shell: bash
